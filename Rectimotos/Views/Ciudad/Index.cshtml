@model IEnumerable<Rectimotos.Clases.Ciudad>

@{
    ViewData["Title"] = "Index Ciudad";
}

<head>
    <title>@ViewData["Title"] - RECTIMOTOS</title>

    <link href="~/lib/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="~/lib/font-awesone/css/all.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link href="https://cdn.datatables.net/1.13.6/css/jquery.dataTables.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="~/css/Indexs.css" />

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.min.js"></script>
    <script src="~/lib/font-awesone/js/all.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
</head>


<body>
    <div class="container-fluid text-center">

        <h4>TABLA DE CIUDADES</h4>

        <div class="row botons col-12">
            <a asp-action="Create" asp-route-idEstado="@ViewBag.Estado" class="btn btn-secondary col-1"><i class="fa-solid fa-user-plus"></i>Crear Ciudad</a>
        </div>

        <table class="table table-striped table-bordered text-center" id="tblCiudad">
            <thead class="thead-dark">
                <tr>
                    <th>CIUDAD</th>
                    <th></th>
                </tr>
            </thead>

            <tbody>
                @foreach (var ciudad in Model)
                {
                    <tr>
                        <td>
                            @ciudad.Nombre
                        </td>
                        <td>
                            <a class="btn btn-primary" asp-action="Edit" asp-route-id="@ciudad.IdCiudad"><i class="fa-solid fa-pencil"></i></a> |
                            <a class="btn btn-danger" asp-action="Delete" asp-route-id="@ciudad.IdCiudad"><i class="fa-solid fa-trash-can"></i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</body>